[pytest]
# tests/conftest.py does `from main import app`, so the repo root must be on
# sys.path. `python -m pytest` adds the CWD implicitly; the bare `pytest`
# console script does NOT, so without this the suite dies at conftest import
# with ModuleNotFoundError: No module named 'main'.
pythonpath = .
testpaths = tests
